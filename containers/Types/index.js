import TypeResource from 'constants/resources/type';
import composeResource from 'helpers/composeResource';
import TableView from 'components/TableView';

export default composeResource(TypeResource)(TableView);
